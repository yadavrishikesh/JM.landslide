% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/other-supporting-functions.R
\name{pbGPD}
\alias{pbGPD}
\title{Cumulative Distribution Function of Mixture of Truncated Beta and GPD}
\usage{
pbGPD(x, u.thr, k, xi, sigma, sigma.GP, thr.prob, log.p = TRUE)
}
\arguments{
\item{x}{A numeric vector of observations at which the CDF needs to be evaluated.}

\item{u.thr}{A positive numeric value specifying the threshold that separates the truncated beta and GPD components.}

\item{k}{A positive numeric value representing the shape1 parameter of the truncated beta distribution.}

\item{xi}{A numeric value representing the shape parameter of the GPD.}

\item{sigma}{A numeric vector specifying the shape2 parameters of the truncated beta distribution; must be of the same length as \code{x}.}

\item{sigma.GP}{A positive numeric value representing the scale parameter of the GPD.}

\item{thr.prob}{A numeric value between 0 and 1 indicating the mixture probability for the GPD component (i.e., the probability that a sample exceeds the threshold \code{u.thr}).}

\item{log.p}{A logical value; if \code{TRUE}, the log of the CDF is returned. If \code{FALSE}, the CDF is returned.}
}
\value{
A numeric vector of the same length as \code{x}, containing the CDF (or log CDF) values of the mixture distribution evaluated at each element of \code{x}.
}
\description{
This function computes the cumulative distribution function (CDF) of a mixture distribution that consists of a truncated beta distribution on the interval \code{(0, u.thr)} and a Generalized Pareto Distribution (GPD) for values exceeding the threshold \code{u.thr}.
}
\details{
The function computes the CDF of a mixture distribution where:
\itemize{
\item For values of \code{x} in the interval \code{(0, u.thr)}, the CDF is derived from a truncated beta distribution with parameters \code{k} and \code{sigma}.
\item For values of \code{x} greater than \code{u.thr}, the CDF is derived from a GPD with location \code{u.thr}, scale \code{sigma.GP}, and shape \code{xi}.
The mixture is determined by \code{thr.prob}, which specifies the probability of being in the GPD component.
}
}
\examples{
# Example usage of pbGPD function
set.seed(123)
x <- seq(0, 10, length.out = 100)
u.thr <- 5
k <- 2
xi <- 0.3
sigma <- rep(1, length(x))
sigma.GP <- 0.8
thr.prob <- 0.3
cdf <- pbGPD(x, u.thr, k, xi, sigma, sigma.GP, thr.prob, log.p = FALSE)
plot(x, cdf, type = "l", main = "CDF of Mixture of Truncated Beta and GPD")
}
\seealso{
\code{\link{pbetat}} for the truncated beta distribution CDF function,
\code{\link[evd]{pgpd}} for the Generalized Pareto Distribution CDF function.
}
