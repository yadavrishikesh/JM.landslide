% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/other-supporting-functions.R
\name{transfo}
\alias{transfo}
\title{Transform Parameter to an Unconstrained Scale}
\usage{
transfo(par, lb, ub)
}
\arguments{
\item{par}{A scalar parameter to be transformed.}

\item{lb}{A scalar specifying the lower bound of the parameter.}

\item{ub}{A scalar specifying the upper bound of the parameter.}
}
\value{
The transformed parameter on an unconstrained scale.
}
\description{
This function transforms a parameter constrained by specified lower and upper bounds to an unconstrained scale. The transformation depends on whether the bounds are finite or infinite.
}
\details{
The transformation is determined by the bounds provided:
\itemize{
\item If both \code{lb} and \code{ub} are infinite, the parameter remains unchanged.
\item If only \code{lb} is finite, a log transformation is applied: \code{log(par - lb)}.
\item If only \code{ub} is finite, a log transformation is applied: \code{log(ub - par)}.
\item If both \code{lb} and \code{ub} are finite, a logistic transformation is applied: \code{qlogis((par - lb) / (ub - lb))}.
}
}
\examples{
# Transform parameters with different bounds
transfo(par = 0.5, lb = -Inf, ub = Inf)  # No transformation
transfo(par = 3, lb = 1, ub = Inf)       # Log transformation with lower bound
transfo(par = 7, lb = -Inf, ub = 10)     # Log transformation with upper bound
transfo(par = 2, lb = 1, ub = 5)         # Logistic transformation between bounds

# Vectorized transformation
params <- c(2, 3, 4)
transfo(par = params, lb = 1, ub = 5)
}
\seealso{
Section 56 of the Stan Reference Manual, version 2.9, provides more details on transformations for unconstrained parameter spaces. \url{https://github.com/stan-dev/stan/releases/download/v2.9.0/stan-reference-2.9.0.pdf}
}
