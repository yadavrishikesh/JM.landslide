% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/other-supporting-functions.R
\name{pEGPD1}
\alias{pEGPD1}
\title{Cumulative Distribution Function of Extended GPD (Type 1)}
\usage{
pEGPD1(x, k, xi, sigma, log = TRUE)
}
\arguments{
\item{x}{A numeric vector of quantiles at which to evaluate the CDF.}

\item{k}{A positive numeric value representing the extension parameter, which modifies the GPD.}

\item{xi}{A numeric value representing the shape parameter of the GPD.}

\item{sigma}{A positive numeric value representing the scale parameter of the GPD.}

\item{log}{A logical value indicating whether to return the log-CDF (default is \code{TRUE}).}
}
\value{
A numeric vector of the CDF values of the extended GPD (Type 1) evaluated at \code{x}.
}
\description{
This function computes the cumulative distribution function (CDF) of the extended Generalized Pareto Distribution (GPD) of type 1.
The extended GPD modifies the standard GPD by raising its CDF to the power \eqn{k}, providing a flexible model for tail behavior.
}
\details{
The extended GPD (Type 1) modifies the standard GPD by raising its cumulative distribution function (CDF)
to the power \eqn{k}. The CDF is given by:
\deqn{F(x; k, \xi, \sigma) = F_{\text{GPD}}(x; \xi, \sigma)^{k}}
where \eqn{F_{\text{GPD}}} is the CDF of the standard GPD.
}
\examples{
# Evaluate the CDF of the extended GPD (Type 1)
x <- seq(0, 10, length.out = 100)
k <- 2
xi <- 0.5
sigma <- 1
cdf <- pEGPD1(x, k, xi, sigma, log = FALSE)
plot(x, cdf, type = "l", main = "Extended GPD (Type 1) CDF", xlab = "x", ylab = "CDF")

}
\seealso{
\code{\link[evd]{pgpd}} for the standard GPD cumulative distribution function.
}
